#!/bin/sh
if [ -f node_modules/rvlib-shipkit/src/cli.ts ]; then
  bun node_modules/rvlib-shipkit/src/cli.ts hooks commit-msg "$1" || exit 1
else
  echo "commit-msg: rvlib-shipkit not installed (run bun install), attribution gate skipped" >&2
fi
exec bun scripts/check-banned.ts --msg "$1"
